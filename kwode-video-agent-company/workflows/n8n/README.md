# n8n workflow blueprints

Reserved for n8n-exported workflow JSON. None ship in MVP — these are
hand-authored as integrations get wired (e.g. Erie.pro provider-onboarding
fan-out, monthly-report digest).

Naming convention: `<flow-name>.workflow.json`.
