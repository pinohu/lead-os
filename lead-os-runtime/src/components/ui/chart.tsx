"use client"

// Chart component stub — will be implemented with full recharts integration
// when chart data is available from the backend

export const ChartContainer = ({ children, className }: { children: React.ReactNode; className?: string }) => (
  <div className={className}>{children}</div>
)

export const ChartTooltip = () => null
export const ChartTooltipContent = () => null
export const ChartLegend = () => null
export const ChartLegendContent = () => null
