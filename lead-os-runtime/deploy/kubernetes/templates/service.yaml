apiVersion: v1
kind: Service
metadata:
  name: {{ include "lead-os.fullname" . }}
  labels:
    {{- include "lead-os.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "lead-os.selectorLabels" . | nindent 4 }}
