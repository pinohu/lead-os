apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lead-os.fullname" . }}-config
  labels:
    {{- include "lead-os.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
